blur-method = "dual_kawase";
blur-strength = 4.5;
backend = "glx";
inactive-opacity = 0.60;
opacity-rule = [
	"75:name *= 'mrquatrelle' && focused",
	"75:name *= 'ist1' && focused",
	"85:name *= 'VIM' && focused",
	"90:name *= 'Discord' && !focused",
	"100:name *= 'Pinta' && !focused"
	"100:name *= 'Brave' && !focused",
	"100:name *= 'Spotify' && !focused",
	"100:name *= '.pdf' && !focused"
];
inactive-opacity-override = false;
